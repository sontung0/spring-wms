# Notes

- User wants to learn Spring Cloud Stream for real-world project/work application.
- User already has familiarity with messaging concepts (like Kafka/RabbitMQ) and Spring Boot, so we can skip basic messaging introductions (like "what is a queue or topic") and focus straight on how Spring Cloud Stream abstracts them.
- We should focus on the modern functional paradigm (`java.util.function`) rather than the legacy annotation-based model (`@StreamListener`, `@EnableBinding`), as this is the standard for modern projects.
- **Preference:** Use Dark Mode for all lesson HTML files. Inline `<code>` elements should not have a background color (using a distinct text color instead for readability).